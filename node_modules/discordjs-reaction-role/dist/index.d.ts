import { Client, EmojiResolvable, MessageReaction, PartialMessageReaction, PartialUser, Snowflake, User } from "discord.js";
export interface ReactionRoleConfiguration {
    messageId: Snowflake;
    roleId: Snowflake;
    reaction: EmojiResolvable;
}
export default class ReactionRole {
    private client;
    private reverseConfig;
    constructor(client: Client, config: ReactionRoleConfiguration[]);
    private extractRole;
    private addReaction;
    removeReaction(reaction: MessageReaction | PartialMessageReaction, user: User | PartialUser): Promise<void>;
    teardown(): void;
}
